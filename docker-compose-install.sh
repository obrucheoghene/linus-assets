# 1 Update package list
sudo apt update

# Install Docker Compose using pip, the Python package manager:

sudo apt-get install python3-pip -y     

sudo pip install --upgrade docker-compose

# Verify the Update
docker-compose --version
